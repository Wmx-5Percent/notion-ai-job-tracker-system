# notion-client MUST be 3.x: the data sources API (data_sources.query /
# parent=data_source_id) does not exist in 2.x. See ADR-0002.
notion-client>=3.1,<4
python-dotenv>=1.0.1
httpx>=0.27
pyyaml>=6.0
